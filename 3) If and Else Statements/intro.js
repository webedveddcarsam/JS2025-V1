/**
 * For decision making we use if and else
 * They are running on th4e base of booleans
 */



/**
 * = assignment operator
 * == equality check while ignoring the data types
 * === stickt equality check keeping in view the data types
 */

let myVar = "Apple"
console.log(10 == "10");
console.log(10 === "10");

const shouldSayHi = true

if(shouldSayHi === false){
    console.log("Hi Eddie");
}