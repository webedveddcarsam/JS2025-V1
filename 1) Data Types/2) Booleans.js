/**
 * Booleans are true or false
 *
 * 0 is considered as false
 * 1 is considered as true
 */

console.log(true);
console.log(false);

// c

// the following always evalaute to true

var isTrue = true;
var isOne = 1; // 1 is true by default //

var nonEmptyString = "Hi Eddie";

// Boolena() to check the status

console.log(Boolean(isTrue));

// the following always evalaute to false

var isFalse = false;
var isZero = 0; // 0 is false by default //
var emptyString = "";
var iSNAN = NaN;
var isUndefined = undefined;
var nonAssigned;

// // Array

var myArray = [1, 4, 5, "Eddie", "Huzaifa", true];

console.log(myArray[3]);

console.log(myArray.length);
